# Repository Instructions

- Make sure to run the tests unless the user explicitly says not to.
- Only add tests when the user asks for them.
- After each complete answer with code changes, provide a good commit message using the Angular commit convention.
